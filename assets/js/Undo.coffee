# this file describes the implementation of a general undo stack in angular

# create the angular module
undo = angular.module 'undo', []

undo.controller 'undoCtrl', [ '$scope', ($scope) ->

  console.log 'starting undo controller'
  
]
